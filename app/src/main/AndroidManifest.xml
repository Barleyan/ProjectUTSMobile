<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.barleyan.managementoko">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ecommerce"
        android:label="Project UTS Mobile"
        android:roundIcon="@mipmap/ecommerce_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project"
        tools:targetApi="31">

        <!-- Main Activity yang akan diluncurkan pertama kali -->
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity untuk mengatur PIN Lock jika fitur keamanan diaktifkan -->
        <activity android:name=".PinLockActivity"
            android:label="Pin Lock"
            android:exported="false"/>

        <!-- Tambahkan activity lain sesuai kebutuhan seperti notifikasi atau lainnya -->

    </application>

</manifest>