<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white"/> <!-- Warna latar belakang -->
    <corners android:radius="8dp"/> <!-- Radius sudut -->
    <padding
        android:left="12dp"
        android:top="12dp"
        android:right="12dp"
        android:bottom="12dp"/> <!-- Padding di dalam EditText -->
    <stroke
        android:width="1dp"
        android:color="@android:color/darker_gray"/> <!-- Warna dan lebar garis tepi -->
</shape>